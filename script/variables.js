import { Variable } from "./module.js";

var speed = new Variable(10);
var scale = new Variable(10);
var width = new Variable(10);
var height = new Variable(10);

export { speed, scale, width, height }